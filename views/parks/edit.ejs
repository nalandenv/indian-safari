<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit National Park Details</title>
</head>
<body>
    <h1>Edit Details</h1>
    <form action="/nationalparks/<%=park._id%>?_method=PUT" method="POST">
        <div>
            <label for="title">Name</label>
            <input type="text" name="title" id="title" value="<%= park.title %> ">

            <label for="location">Location (State)</label>
            <input type="text" name="location" id="location" value="<%= park.location %> ">
        </div>
        <br>
        <div>
            <label for="entry">Entry fee</label>
            <input type="text" id="entry" name="entry" value="<%= park.entry %> ">
        </div>
        <br>
        <div>
            <label for="description">Description</label>
            <textarea name="description" id="description" cols="30" rows="5"><%= park.description %> </textarea>
        </div>
        <button>Submit</button>
    </form>
    <a href="/nationalparks">All National Parks</a>
</body>
</html>